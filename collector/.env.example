# Optional — overrides the default (Cloudflare tunnel, wss://agent.murder-pitwall.com).
# Set these for LAN/dev use instead of the tunnel.
# AGENT_HOST=localhost
# AGENT_PORT=4100

# Optional overrides (defaults shown)
# TELEMETRY_POLL_RATE_MS=16
# SESSION_POLL_RATE_MS=1000
# RECONNECT_INITIAL_DELAY_MS=1000
# RECONNECT_MAX_DELAY_MS=30000
# RECONNECT_BACKOFF_MULTIPLIER=2